package cz.gyarab.prg2.s1.malovani;

/*
import javafx.scene.shape.LineTo;
import javafx.scene.shape.MoveTo;
import javafx.scene.shape.Path;
*/

public class Obdelnik extends Nic {
/*    int startX;
    int startY;
    int velikostX;
    int velikostY;

    public Obdelnik(int startX, int startY, int velikostX, int velikostY) {
        this.startX = startX;
        this.startY = startY;
        this.velikostX = velikostX;
        this.velikostY = velikostY;
    }


    public Path namalujSe() {
        Path path = new Path();
        MoveTo moveTo = new MoveTo(startX, startY);

        LineTo line1 = new LineTo(startX+velikostX, startY);   // cara 1
        LineTo line2 = new LineTo(startX+velikostX, startY+velikostY); // cara 2
        LineTo line3 = new LineTo(startX,startY+velikostY);  // cara 3
        LineTo line4 = new LineTo(startX, startY);              // cara 4

        path.getElements().add(moveTo);
        path.getElements().addAll(line1, line2, line3, line4);

        return path;
    }

 */
}
